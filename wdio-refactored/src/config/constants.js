/**
 * Common Constants
 * Centralized constants for the TAF
 */

// Timeout constants (in milliseconds)
export const TIMEOUTS = {
  DEFAULT: 10000,
  SHORT: 5000,
  MEDIUM: 15000,
  LONG: 30000
};

// Common selectors
export const SELECTORS = {
  TOAST: '.ngx-toastr, .toast'
};

// Error messages
export const ERROR_MESSAGES = {
  LOGIN_FAILED: 'Login did not complete',
  PAGE_LOAD_FAILED: 'Page did not load',
  PRODUCTS_NOT_LOADED: 'Products did not load',
  CART_NOT_UPDATED: 'Cart did not update'
};
